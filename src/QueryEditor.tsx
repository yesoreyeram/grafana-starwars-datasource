import React from 'react';
import { QueryEditorProps } from '@grafana/data';

interface StarWarsQueryEditorProps extends QueryEditorProps<any> {}

export const StarWarsQueryEditor = (props: StarWarsQueryEditorProps) => {
  return <>Query Editor</>;
};
